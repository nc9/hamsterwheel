/**
 * @hamsterwheel/gate
 *
 * The pure, side-effect-free merge-gate kit lands here. Planned surface:
 *   - `mergeDecision`        — should this branch be allowed to merge?
 *   - rubric/CI reconciliation — fold a review rubric together with CI results
 *   - an implement-outcome classifier — did the agent actually finish the task?
 *   - an injection screen     — flag untrusted agent output before it's trusted
 *   - WIP salvage             — never lose half-finished overnight work
 *
 * Every function here is meant to be pure and heavily tested. Nothing yet.
 */

/** Placeholder marker until the merge-gate kit is implemented. */
export const GATE_TODO = true;
